# AWS Configuration
# Rename this file to .env and add your actual credentials
# WARNING: Never commit the actual .env file with credentials to git

# AWS Credentials
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_REGION=us-east-1

# API Configuration
REACT_APP_API_ENDPOINT=https://your-api-endpoint.execute-api.us-east-1.amazonaws.com/dev